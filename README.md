This is a local AI chatbot for your home, feel free to change it in any way you like.
do 'pip install -r requirements.txt' in vscode
make sure to install ollame by running "curl -fsSL https://ollama.com/install.sh | sh" in your terminal if ur on linux.
if you're on windows or mac, just go to the ollama website and click download for windows/mac
to install a model on any OS, run: "ollama pull (modelname)" in your terminal. Then run: "ollama run (model you downloaded)" and it should run in your terminal. Use the code to insert it into a python file to give it longer memory and a fully customizable template.
